# Agent Rules

## Git Workflow
- Keep all Git commits local by default (`git commit`).
- Do NOT automatically run `git push` after making commits.
- Only execute `git push` when the user explicitly requests a push or triggers a manual push action.
